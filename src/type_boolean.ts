

// Type boolean
let autenticado: boolean = true;


let codeStatus: String = 'a';

autenticado = false;

//Tudo que for diferente de zero, string vazia, undefined, será verdadeiro.
autenticado = Boolean(codeStatus);

console.log(autenticado);
